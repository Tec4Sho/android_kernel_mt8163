analyze:
  - --disable-all
  - --analyzers=clang-tidy
  - --analyzer-config=clang-tidy:take-config-from-directory=true
  - --no-missing-checker-error
  - --report-hash=diagnostic-message
  - --verbose=info
  - --clean
  - --stats

parse:
  - --trim-path-prefix=$HOME/workspace
  - --print-steps
